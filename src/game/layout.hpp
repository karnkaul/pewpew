#pragma once
#include <glm/vec2.hpp>

namespace pew::layout {
inline constexpr auto nWorldArea = glm::vec2(1.0f, 0.8f);
inline constexpr auto playerQuad = glm::vec2(100.0f, 100.0f);
inline constexpr auto projectileQuad = glm::vec2(100.0f, 5.0f);
} // namespace pew::layout
